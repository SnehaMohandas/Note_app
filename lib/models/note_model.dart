class Note {
  String id;
  String title;
  String description;
  DateTime datetime;

  Note({
    required this.id,
    required this.title,
    required this.description,
    required this.datetime,
  });
}
